import { expect } from "chai";
import { ethers } from "hardhat";
// import { GrothV}

describe("Addition", () => {
  let Verifier;
  beforeEach(() => {
    ethers.getContractFactory(AdditionV);
  });
});
